#!/usr/bin/env bash
# macOS: dê DOIS CLIQUES neste arquivo para abrir o app local (dashboard + chat).
# (Se o macOS bloquear, clique com o botão direito → Abrir → Abrir.)
cd "$(dirname "$0")"
exec bash ./start.sh
